# Optional CUDA environment for new AAA work (aaa.1k.v2 and later). CPU-only
# installation, CI and every historical phase use requirements-lock.txt alone;
# that file is part of the aaa.1k.v1 scientific identity and is not changed by
# adding this one. This lock repeats every base pin unchanged (tested) and adds
# the CUDA packages the FLOWBOX qualification used:
#
#   python -m venv .venv-cuda && . .venv-cuda/bin/activate
#   python -m pip install -r requirements-cuda-lock.txt
#   python -m pip install -e . --no-deps
#   python tools/check_lock.py --lock requirements-cuda-lock.txt
#
# The CUDA runtime components come from NVIDIA's PyPI wheels, so no system CUDA
# toolkit is needed; an NVIDIA driver supporting CUDA 13.x is. See docs/hardware.md
# and docs/aaa_1k_v2_compute_report.md.
#
# Runtime
matplotlib==3.11.1
numpy==2.5.3

# CUDA backend (CuPy) and its NVIDIA runtime wheels
cupy-cuda13x==14.2.0
cuda-pathfinder==1.8.2
cuda-toolkit==13.2.2
nvidia-cublas==13.4.1.3
nvidia-cuda-nvrtc==13.2.86
nvidia-cuda-runtime==13.2.86

# Development tooling (formatter, linter, type checker, coverage)
build==1.6.1
coverage==7.16.0
mypy==2.3.1
ruff==0.16.7
setuptools==84.0.0

# Transitive
ast_serialize==0.11.1
contourpy==1.3.3
cycler==0.12.1
fonttools==4.65.0
kiwisolver==1.5.1
librt==0.15.0
mypy_extensions==1.1.0
packaging==26.3
pathspec==1.1.1
pillow==12.3.0
pyflakes==3.4.0
Pygments==2.21.0
pyparsing==3.3.2
pyproject_hooks==1.2.0
python-dateutil==2.9.0.post0
six==1.17.0
typing_extensions==4.16.0
